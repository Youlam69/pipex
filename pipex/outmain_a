#include <sys/wait.h>
int main()
	opn = open("hilol.tsasat", O_RDWR);
		perror("wasat");
		printf("sat hadi hia %s\n", strerror(errno));
			write(opn, "abcd",4);
	wait(NULL);
	wait(NULL);
	// char *sort;
	// sort = malloc(4444);
	// if(ac < 1)
	// 	write(fd[1], "haliol", 6);
	// 	rtr = read(fd[0], sort, sizeof(sort));
	// 	printf("hada rtr %d\n", rtr);
	// 	printf("hada sort %s\n", sort);
	// 	rtr = read(fd[0], sort, sizeof(sort));
	// 			printf("hada rtr 2 %d\n", rtr);
	// 	printf("hada sort  1 %s\n", sort);
	// 	wait(NULL);
